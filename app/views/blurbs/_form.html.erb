<%= form_with(model: blurb, local: true) do |form| %>

  <% if blurb.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blurb.errors.count, "error") %> prohibited this blurb from being saved:</h2>

      <ul>
      <% blurb.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  

  <div class="form-group">
    <%= form.label :content %>
    <%= form.text_area :content, id: :blurb_content, class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit t('.sign_up', default: "Add Blurb"), class: "btn btn-success" %>
  </div>


<% end %>

